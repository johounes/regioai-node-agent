{
  "name": "cag-node-agent",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "description": "Community AI Grid – Node-Agent (wraps Ollama, sends heartbeats)",
  "scripts": {
    "start": "node agent.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
